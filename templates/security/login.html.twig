{% extends 'base.html.twig' %}

{% block title %}Se connecter{% endblock %}

{% block body %}

{#
    Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
    See https://symfony.com/doc/current/security/remember_me.html

    <div class="checkbox mb-3">
        <label>
            <input type="checkbox" name="_remember_me"> Remember me
        </label>
    </div>
#}

<section class="section register" id="register" aria-label="register">
        <div class="container flex">
            <div class="register-form">
                <form method="post">
                    <h2 class="h2">Se connecter</h2>
                    <div class="register-input">
                        <div>
                            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                            <label for="inputEmail">Email</label>
                        </div>
                        <div>
                            <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                            <label for="inputPassword">Mot de passe</label>
                            {% if error %}
                                <span class="register-error">{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                            {% endif %}
                        </div>
                    </div>
                    
                    <div class="register-btn">
                        <div>
                            <button type="submit">Se connecter</button>
                        </div>
                    </div>
                </form>
                <div class="register-btn">
                    <div>
                        <p>Ou <a href="{{path('app_register')}}" class="register-a">créer un compte</a></p>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
